extends layout

block content
  p Fill the information(s) below to create new genre:

  form(method='POST' action='')
    div.form-group
      label(for='name') Genre:
      input#name.form-control(type='text', placeholder='Adventure, Poetry, Politics etc.' name='name' value=(undefined===genre ? '' : genre.name) required='true')
    button.btn.btn-primary(type='submit') submit

  if errors 
   ul
    for error in errors
     li!= error.msg

     